import { Component, OnInit } from '@angular/core';
import { ActivatedRoute, Router } from "@angular/router";
import { FormArray, FormBuilder, FormControl, Validators } from '@angular/forms';

import { MatDialog } from '@angular/material/dialog';

import { T3Service } from '../t-3.service';
import { IT3 } from '../it-3';

import { BaseDetailsComponent, FieldType, PickerDialogService } from 'src/app/common/shared';
import { ErrorService } from 'src/app/core/services/error.service';


@Component({
  selector: 'app-t-3-details',
  templateUrl: './t-3-details.component.html',
  styleUrls: ['./t-3-details.component.scss']
})
export class T3DetailsComponent extends BaseDetailsComponent<IT3> implements OnInit {
	title = 'T3';
	parentUrl = 't3';
	constructor(
		public formBuilder: FormBuilder,
		public router: Router,
		public route: ActivatedRoute,
		public dialog: MatDialog,
		public t3Service: T3Service,
		public pickerDialogService: PickerDialogService,
		public errorService: ErrorService,
	) {
		super(formBuilder, router, route, dialog, pickerDialogService, t3Service, errorService);
  }

	ngOnInit() {
		this.entityName = 'T3';
		this.setAssociations();
		super.ngOnInit();
		this.setForm();
    	this.getItem();
	}
  
  setForm(){
    this.itemForm = this.formBuilder.group({
      id: [{value: '', disabled: true}, Validators.required],
	  score: new FormArray([]),
      
    });
    
    this.fields = [
        {
		  name: 'score',
		  label: 'score',
		  isRequired: false,
		  isAutoGenerated: false,
                 type : FieldType.Array,
                 subtype : FieldType.Number,
	    },
      ];
      
  }
  
  onItemFetched(item: IT3) {
    this.item = item;
	 this.item.score.forEach(elem => {
          (<FormArray>this.itemForm.get('score')).push(new FormControl(elem));
      });
        delete item.score;
     this.itemForm.patchValue(item);

  }
  
  setAssociations(){
    this.associations = [
		];
		
		this.childAssociations = this.associations.filter(association => {
			return (association.isParent);
		});

		this.parentAssociations = this.associations.filter(association => {
			return (!association.isParent);
		});
	}
	
	onSubmit() {
		let t3 = this.itemForm.getRawValue();
		super.onSubmit(t3);
		
	}
}
